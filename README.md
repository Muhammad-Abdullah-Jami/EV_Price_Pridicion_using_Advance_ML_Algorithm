Here i trained multiple machine learning models and evalute them with parameter tuning then model with best results was seleceted for price prediction of the
EV's based on the features that the user provided
